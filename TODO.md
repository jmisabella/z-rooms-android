# TODO

- For upcoming release of version 2.5 which adds optional voices, we must adjust the Play Store description to specify the following: "Optional enhanced meditation voices can be downloaded separately through Android (requires additional storage)"
